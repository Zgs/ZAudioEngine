internal enum VirtualCheckType
{
    None,
    PlaybackCount,
    MaxWithinRadius,
    Both
}

internal enum SoundType
{
    None,
    Music,
    Effect
}

internal enum PlayType
{
    None,
    Loop,
    OneShot
}
